# rule/spec-cache/ holds byte snapshots of Atlassian's official OpenAPI
# specs. They contain documentation "example" values (e.g. fake webhook
# tokens) that trip push protection as false positives; real credentials
# never live here.
paths-ignore:
  - "rule/spec-cache/**"
